# Project-specific skills

Place repository-specific workflows here when a task requires project knowledge that should be version-controlled.

Example:

`.cursor/skills/deploy-staging/SKILL.md`

Global reusable skills should remain in `~/.cursor/skills/`.
